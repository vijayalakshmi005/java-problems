class pg{
    public static void main(String[]args)
    {
        int n,i,count=0;
        n=5;
        for(i=1;i<=n;i++)
        {
            if(n%i==0)
            count++;
        }
        if(count==2)
        {
            System.out.println("it is a prime");
        }
        else 
        {
            System.out.println("not a prime");
        }
    }
}

OUTPUT:
it is a prime

DETAILED DESCRIPTION:
the program determines whether the given number is a prime or not in which it uses the condition n%i==0 and using if condition it check the value equal to 2 and determine whether prime or  not.
